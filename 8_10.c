#include <stdio.h>

int main()
{
	int ch = getchar();
	printf("%d\n", ch);
	putchar(ch);
	/*int n = 1;
	while(n <= 10)
	{
		printf("%d\t", n);
		n++;
	}

	for (int m=1; m <= 10; m ++ )
	{
		printf(" % d\t", m);
	}*/
	return 0;
}
